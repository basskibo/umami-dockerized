## Umami docker compose

Ready to use umami docker compose file where you can download and use it as is.

- Clone project

```
cd umami-dockerized
```

- Run docker compose

```
docker compose up
or
docker compose up -d
```
